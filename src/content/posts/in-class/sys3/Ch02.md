---
title: 'Ch02 - 指令级并行 (ILP)'
pubDate: 2026-04-10
author: 'Acola'
category: '课业'
tags: ['Sys3', 'Computer Science']
---

## 一、指令级并行 (ILP) 基础
- **定义**：指指令之间存在的并行性，硬件通过重叠执行多条指令来提高性能。
- **核心目标**：降低 **CPI** (Cycles Per Instruction)。
- **流水线冲突 (Pipeline Hazards)**：
    1.  **结构冲突 (Structural)**：硬件资源竞争（如内存端口不足）。
    2.  **数据冲突 (Data)**：指令执行依赖于前序指令的结果。
    3.  **控制冲突 (Control)**：由分支指令（Branch）引起，改变了指令流。

## 二、数据相关性与名相关 (Dependences)
在乱序执行中，必须识别并处理以下相关性：
- **数据相关 RAW**：指令 B 需要指令 A 的计算结果。这是“真相关”，必须等待。
- **名相关 (Name Dependence)**：指令使用相同的寄存器名，但并无数据流传递。
    - **WAR**：指令 B 写寄存器，指令 A 读该寄存器。
    - **WAW**：指令 A 和 B 同时写同一个寄存器。
- 其中，RAW 无法解放并行性，WAR 和 WAW 可以解放并行性。
- **解决方法**：**寄存器重命名 (Register Renaming)** 是消除名相关的主要手段。
    - **具体操作分为以下四步**：
    1. **分配新物理寄存器**：遇到写寄存器指令，分配一个空闲物理寄存器。
    2. **更新映射表**：把架构寄存器指向新物理寄存器，旧映射暂存不删除。
    3. **读写都走物理寄存器**：读（查映射表取旧值）写（只写新物理寄存器，不污染原值）。
    4. **顺序提交并覆盖架构状态**：按程序顺序提交，提交后才将新物理寄存器永久生效，旧寄存器释放。
    注：永久生效 = 让架构寄存器（软件可见的 A1、A2…）真正更新为新值，对后续所有指令可见。


## 三、动态调度：Scoreboard 记分牌算法
在原本的流水线设计中，ID 阶段同时进行了结构冲突和数据冲突的检测。
而记分牌算法将其拆成 **IS（检测结构冲突）** 和 **RO（检测数据冲突）** 两个步骤。

<img src="/images/in-class/sys3/Ch02-1.png"/>

### 1. 具体过程
1. **Issue（IS）**：顺序发，检查结构冒险
2. **Read Operands（RO）**：等数据就绪 → 解决 RAW
3. **Execute（EX）**：乱序算
4. **Write Result（WB）**：按顺序写回

### 2. 具体举例

<img src="/images/in-class/sys3/Ch02-2.png"/>

由于第 4 条指令正在使用唯一的加法器，因此第 6 条指令出现结构冲突，IS 卡住
对于第 3, 4, 5 条指令，均出现 RAW 情况，需要等待之前的数据写回，出现数据冲突，RO 卡住

<img src="/images/in-class/sys3/Ch02-3.png"/>

这张表记录硬件状态。
其中 Fi 代表结果数，Fj, Fk 代表源数据。相应的 Q 代表源数据来自哪个硬件，R 代表是否准备好。

<img src="/images/in-class/sys3/Ch02-5.png"/>

这张表为寄存器表。以上三张表为记分牌算法所需的三张表（指令，硬件，寄存器）

<img src="/images/in-class/sys3/Ch02-4.png"/>

接下来，由于乘除法计算时间较长，第 4 条指令比第 3 条指令先写回。此时第 6 条的 IS, RO 检测均通过。
自然的出现了乱序执行。


### 3. 总周期计算
**重要理解**：
1. IS：串行处理，逐条分配资源，只能顺序检测。
   RO：并行等待，多条指令可同时就绪，统一检测。
2. 写回顺序要求：对于同一寄存器，确保前面的指令读/写完成才能写回。

Add instruction needs 2 clock cycles. Multiply instruction needs 10 clock cycles.
Division instruction needs 40 clock cycles. LD instruction need 1 clock cycles.

<img src="/images/in-class/sys3/Ch02-6.png"/>

**图中易错点**：
1. IS 串行，不能同时进行。RO 并行，可以同时进行
2. 第 6 条的 WB 要等待第 5 条的 RO 完成（等第 5 条读完 F6）

## 四、动态调度：Tomasulo 算法
Tomasulo 算法通过硬件实现了**乱序执行 (Out-of-Order Execution)**，其核心思想是只要操作数就绪，指令就可以执行。

<img src="/images/in-class/sys3/Ch02-7.png"/>

### 1. 核心组件
1.  **保留站 (Reservation Stations, RS)**：
    - 缓冲待执行的指令。
    - 存储已就绪的操作数或记录产生操作数的“源头”。
2.  **公共数据总线 (Common Data Bus, CDB)**：
    - 所有执行单元的结果都广播到 CDB 上。
    - RS 和寄存器通过监听 CDB 直接获取数据，绕过寄存器堆（隐式寄存器重命名）。
3.  **Load/Store 缓冲区**：处理内存访问。

### 2. 运行三阶段
1.  **Issue (发射)**：从指令队列取出指令。如果有空闲的 RS，则发射；同时进行重命名。
2.  **Execute (执行)**：如果在 RS 中操作数未就绪，则监听 CDB；一旦就绪，开始执行。
3.  **Write Result (写回)**：计算完成，通过 CDB 广播结果，更新 RS 和寄存器。

### 3. 具体举例

<img src="/images/in-class/sys3/Ch02-8.png"/>

Tomasulo 算法没有 RO 阶段，它通过直接存储操作数或源头的方式进行隐式重命名。

<img src="/images/in-class/sys3/Ch02-9.png"/>

Vj, Vk 直接记录操作数。最后一列的 A（address）用来记录 load/store 指令的地址。

<img src="/images/in-class/sys3/Ch02-10.png"/>

以上三张表，就是 Tomasulo 算法的三张表（指令，保留站，寄存器）

### 4. 总周期计算
Tomasulo 的 EX 和 WB 中间不存在等待，并且也没有写回顺序要求，数据来了就算，算完就写。
<img src="/images/in-class/sys3/Ch02-11.png"/>

值得注意的是，第 1,2 条 load 指令在 EX 阶段要多算一步 A（address），因此多一个周期。

## 五、基于硬件的猜测执行 (Hardware-Based Speculation)
为了处理分支预测并进一步挖掘并行性，系统在 Tomasulo 基础上增加了猜测执行。

### 1. 重排序缓冲区 (Reorder Buffer, ROB)
- **作用**：保存已完成执行但尚未“提交”的指令结果。
- **关键特性**：**乱序执行，顺序提交 (Commit)**。
- **四阶段流程**：
    1.  **Issue**：分配 RS 和 ROB 条目。
    2.  **Execute**：执行计算。
    3.  **Write Result**：将结果写入 ROB 并在 CDB 广播（此时仍是猜测结果）。
    4.  **Commit**：当指令到达 ROB 头部且预测正确，才将结果真正写入寄存器/内存。


## 2. 优势
- **异常处理**：只有指令真正提交时才会触发异常，避免了猜测执行路径上的错误异常。
- **预测失败恢复**：如果分支预测失败，直接清空 ROB 中相关的条目即可，不会污染架构状态。

## 六、分支预测技术 (Branch Prediction)
- **动态分支预测**：
    - **BHT (Branch History Table)**：存储分支历史（1-bit 或 2-bit）。2-bit 状态机更鲁棒。
    - **BTB (Branch Target Buffer)**：存储分支的目标地址，实现零延迟跳转。
- **前瞻取指**：结合预测技术，在时钟周期内预取多条指令。

## 七、多发射技术 (Multiple Issue)
为了使 CPI < 1（即每个周期执行超过 1 条指令）：
- **超标量 (Superscalar)**：硬件在运行时动态决定发射哪些指令。
- **超长指令字 (VLIW)**：编译器在编译阶段静态决定哪些指令可以并行，并将它们打包。

## 八、总结：ILP 的局限性
- 虽然 ILP 技术极大地提升了单核性能，但受到**功耗墙 (Power Wall)**、**复杂性递增**和**指令流固有相关性**的影响，硬件设计重心已逐渐转向多核并行。
