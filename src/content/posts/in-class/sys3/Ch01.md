---
title: 'Ch01 - 计算机系统 Ⅲ 基础'
pubDate: 2026-04-10
author: 'Acola'
category: '课业'
tags: ['Sys3', 'Computer Science']
---


## 一、 计算机系统 Ⅲ

<img src="/images/image.png"/>

<img src="/images/image-1.png"/>

## 二、 性能定量分析 (Quantitative Principles of Performance)
### 1. 性能的定义
**性能 (Performance) = 1 / 执行时间 (Execution Time)**。
**响应时间 (Response Time/Latency)**：从任务开始到完成的时间。
**吞吐量 (Throughput)**：单位时间内完成的总任务量。

### 2. CPU 性能公式 (The Classic CPU Performance Equation)
> $$\text{CPU Time} = \text{Instruction Count (IC)} \times \text{CPI} \times \text{Clock Cycle Time } (T_c)$$

- **IC (指令数)**：由算法、程序设计、编译器和 ISA 决定。
- **CPI (每条指令周期数)**：由硬件实现（微架构）和 ISA 决定。
- **Clock Cycle Time ($T_c$)**：由硬件工艺和流水线设计决定。



### 3. 阿姆达尔定律 (Amdahl’s Law)
**核心定义**：对系统中某一部分进行改进所获得的性能提升，受限于该部分被使用的频率。

> $$\text{Speedup} = \frac{1}{(1 - \text{Fraction}_{\text{enhanced}}) + \frac{\text{Fraction}_{\text{enhanced}}}{\text{Speedup}_{\text{enhanced}}}}$$

**公式解析**：
1. $\text{Fraction}_{\text{enhanced}}$ 指可被优化/并行化部分的占比。例如 80% 的部分可以被优化，则为 0.8
2. $\text{Speedup}_{\text{enhanced}}$ 指可优化部分被优化多少倍。例如可被优化 4 倍，则为 4
3. 按照这个例子，代入公式为：
> $$\text{Speedup} = \frac{1}{(1 - 0.8) + \frac{0.8}{4}} = 2.5$$
1. 因此，极限情况，加入可以无限优化，则 $\text{Speedup}$ 最大为 $\frac{1}{1 - \text{Fraction}_{\text{enhanced}}}$

**结论**：加速比受限于“无法增强的部分”。
**推论**：应当“加速经常性事件 (Make the common case fast)”。



## 三、 计算机架构的八大思想 (8 Architecture Ideas)
1.  **面向摩尔定律设计**：预测未来工艺。
2.  **使用抽象简化设计**：分层管理复杂性。
3.  **加速经常性事件**：性能优化的重点。
4.  **通过并行提高性能**：如多核、向量处理。
5.  **通过流水线提高性能**：重叠执行指令。
6.  **通过预测提高性能**：如分支预测。
7.  **存储器层次结构**：利用局部性原理（Cache）。
8.  **通过冗余提高可靠性**：容错处理。
